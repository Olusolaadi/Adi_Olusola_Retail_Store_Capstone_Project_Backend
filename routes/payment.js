import express from "express";
//import { handleCheckout, handleCheckoutSuccess } from "../controllers/payment.js";

const router = express.Router();

//router.post("/handle_checkout", handleCheckout);
//router.post("/handle_checkout_success", handleCheckoutSuccess);

export default router;